import type { FlowState } from "@/lib/flow/store"

export function selectEditorCanvasState(state: FlowState) {
  return {
    addNodeFromPalette: state.addNodeFromPalette,
    addPrimitiveToNodeNetwork: state.addPrimitiveToNodeNetwork,
    addWorkflowNodeFromCatalog: state.addWorkflowNodeFromCatalog,
    applyWorkflowCapabilities: state.applyWorkflowCapabilities,
    attachToParent: state.attachToParent,
    autoLayout: state.autoLayout,
    clearHelperLines: state.clearHelperLines,
    copy: state.copy,
    cut: state.cut,
    deleteSelected: state.deleteSelected,
    detachFromParent: state.detachFromParent,
    disconnectNodeConnections: state.disconnectNodeConnections,
    duplicate: state.duplicateSelected,
    edges: state.edges,
    enterNodeNetwork: state.enterNodeNetwork,
    exitNodeNetwork: state.exitNodeNetwork,
    groupSelection: state.groupSelection,
    helperLines: state.helperLines,
    insertNodeOnEdge: state.insertNodeOnEdge,
    lockNodeInternals: state.lockNodeInternals,
    networkStack: state.networkStack,
    nodes: state.nodes,
    connectNodes: state.connectNodes,
    onConnect: state.onConnect,
    onReconnect: state.onReconnect,
    onEdgesChange: state.onEdgesChange,
    onNodesChange: state.onNodesChange,
    paste: state.paste,
    redo: state.redo,
    removeEdgesByIds: state.removeEdgesByIds,
    resizeGroupToFit: state.resizeGroupToFit,
    resolveNodeCollisions: state.resolveNodeCollisions,
    save: state.save,
    selectConnectedComponent: state.selectConnectedComponent,
    setToolMode: state.setToolMode,
    takeSnapshot: state.takeSnapshot,
    toolMode: state.toolMode,
    undo: state.undo,
    unlockNodeInternals: state.unlockNodeInternals,
    updateWorkflowProfile: state.updateWorkflowProfile,
    workflowProject: state.workflowProject,
  }
}
